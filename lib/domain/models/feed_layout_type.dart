/// Controls how the main post feed is rendered inside the SDK.
enum FeedLayoutType {
  /// Full-screen vertical reels with overlay actions (default).
  reels,

  /// Scrollable feed with post cards (header, media, actions, caption).
  postFeed,
}
