enum ImageType {
  asset,
  svg,
  file,
  network;
}

enum ButtonType {
  primary,
  secondary,
  outlined,
  text,
  icon;
}

enum GradientTextType {
  linear,
  radial,
  sweep;
}

enum NetworkRequestType {
  get,
  post,
  put,
  patch,
  delete,
}

enum LoaderType {
  normal,
  withBackGround,
  withoutBackground,
  animation,
}

enum SavedValueDataType {
  string,
  int,
  double,
  bool,
  stringList,
}

enum ErrorViewType {
  none,
  dialog,
  snackBar,
  toast,
  bottomSheet,
}

enum PostType {
  video,
  photo,
}

enum MediaSource {
  gallery,
  camera,
}
