import 'package:ism_video_reel_player/ism_video_reel_player.dart' as isr;

/// Optional host overrides for [isr.IsrPostTapHandler].
///
/// When not configured, the SDK uses [isr.IsrPostDetailsActions] defaults
/// (edit flow + delete confirmation + delete API).
class PostTapDemo {
  PostTapDemo._();
}
